# Helpers

Designer-facing inspector/utility helpers. Generic utility context.

## Key files

- `Runtime/RangedFloat.cs` + `Runtime/MinMaxRangeAttribute.cs` — min/max float range with a custom slider drawer (used by Audio for randomized pitch/volume).

## Used by

- **Audio** and any SO needing a ranged value.
